spring:
  datasource:
    url: jdbc:h2:mem:warehouse;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false
firefly:
  security:
    enabled: false
  jwt:
    secret: firefly-logistics-test-secret-must-have-32-bytes
    issuer: firefly-logistics-test
    audience: firefly-logistics-test-web
